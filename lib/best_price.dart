import 'package:flutter/material.dart';

class BastPricePage extends StatelessWidget {
  const BastPricePage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("最安値を調べる"));
  }
}
